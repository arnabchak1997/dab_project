# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 5c86fbc1-1267-4c6a-a6f4-25dfbcfefd8a

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
    default: "citibike_dev"
  dev_flag:
    default: true

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616450018121.1.azuredatabricks.net
    variables:
      catalog: citibike_dev
    presets:
      artifacts_dynamic_version: true

  test:
    mode: production
    workspace:
      host: https://adb-7405615467031513.13.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/arnabsmi@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: "citibike_test"
      dev_flag: false
    permissions:
      - user_name: arnabsmi@gmail.com
        level: CAN_MANAGE
    run_as:
      user_name: arnabsmi@gmail.com

  prod:
    mode: production
    workspace:
      host: https://adb-7405618989580976.16.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/arnabsmi@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: "citibike_prod"
      dev_flag: false
    permissions:
      - user_name: arnabsmi@gmail.com
        level: CAN_MANAGE
    run_as:
      user_name: arnabsmi@gmail.com